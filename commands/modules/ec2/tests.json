{
  "ec2": {
    "create": {
      "enabled": false,
      "example": "kc ec2 create --no-prompt --ec2_name name --ec2_instance_type instance_type --ec2_ami_id ami_id --ec2_key_name key_name --ec2_vpc_id vpc_id --ec2_availability_zone availability_zone --ec2_subnet_id subnet_id --ec2_vpc_security_group_ids vpc_security_group_ids --ec2_role_name role_name --ec2_volume_size volume_size --ec2_user_data user_data --ec2_enable_trend enable_trend --ec2_trend_workload_group trend_workload_group",
      "note": "Add tests in the `tests` list below. Each test must be a valid RaidCloud command. Don't forget to add `--no-prompt`",
      "tests": []
    },
    "list_unmanaged": {
      "enabled": false,
      "example": "kc ec2 list_unmanaged --no-prompt",
      "note": "Add tests in the `tests` list below. Each test must be a valid RaidCloud command. Don't forget to add `--no-prompt`",
      "tests": []
    },
    "enable_trend": {
      "enabled": false,
      "example": "kc ec2 enable_trend --no-prompt --ec2_target target --ec2_instance_id instance_id --ec2_launch_template_id launch_template_id --ec2_enable_trend enable_trend --ec2_trend_workload_group trend_workload_group",
      "note": "Add tests in the `tests` list below. Each test must be a valid RaidCloud command. Don't forget to add `--no-prompt`",
      "tests": []
    },
    "list_launch_templates": {
      "enabled": false,
      "example": "kc ec2 list_launch_templates --no-prompt",
      "note": "Add tests in the `tests` list below. Each test must be a valid RaidCloud command. Don't forget to add `--no-prompt`",
      "tests": []
    }
  }
}